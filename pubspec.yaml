name: proyecto_spotify
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  go_router: ^13.2.0
  provider: ^6.1.2
  google_fonts: ^6.2.1
  just_audio: ^0.10.5
  firebase_core: ^4.4.0
  firebase_auth: ^6.1.4
  cloud_firestore: ^6.1.2
  image_picker: ^1.2.1
  firebase_storage: ^13.1.0
  cloud_functions: ^6.0.0
  http: ^1.2.2
  cached_network_image: ^3.4.1
  uuid: ^4.5.3
  just_audio_background: ^0.0.1-beta.17
  palette_generator: ^0.3.3+7
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  connectivity_plus: ^6.0.5
  path_provider: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true
  assets:
    - assets/logo_hsound.png
    - assets/grecas/

flutter_icons:
  android: true
  ios: true
  image_path: "assets/logo_hsound.png"